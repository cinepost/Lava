#include "$HH/soho/overrides/ViewportRender"

// Viewport overrides for Lava
{
    override.inherit	CommonView
    override.name	    Lava

    // Force the render to go to the "ip" device
    lv_picture		            [ "ip" ]
    lv_image_mplay_framemode    [ "append" ]
    lv_image_mplay_rendermode   [ "current" ]
    lv_rendersource	            [ "SceneView" ]
    lv_device		            [ "" ]
}
