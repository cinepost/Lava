#include "$HH/soho/overrides/RenderCommand"

// Viewport overrides for Lava
{
    override.inherit	CommonCommand
    override.name	Lava

    // Force the render to go to the "ip" device
    lv_picture			[ $over_file ]
    lv_device			[ $over_device ]
}
