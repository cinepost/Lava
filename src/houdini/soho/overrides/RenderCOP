#include "$HH/soho/overrides/RenderCOP"

// Viewport overrides for Lava
{
    override.inherit	CommonCommand
    override.name	    lava

    // Force the render to go to the "ip" device
    lv_picture		[ $imagefile ]
    //lv_device		[ Houdini ]
}
