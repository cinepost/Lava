# Locate Houdini's libraries and header files.
# Registers an imported library target named 'Houdini'.

#add_subdirectory( ropLava ) # Houdini render node

install(DIRECTORY soho DESTINATION ${HOUDINI_USER})